#ifndef PASSWORD_ENTRY_H
#define PASSWORD_ENTRY_H

#include <string>

struct PasswordEntry {
    std::string service;
    std::string username;
    std::string password;
};

#endif
#pragma once
